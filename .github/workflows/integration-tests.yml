name: integration tests for notee client and node

on:
  workflow_dispatch:
  schedule:
    - cron:  '42 3 * * *'
env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: init-rust-target
      run: rustup target add wasm32-unknown-unknown
    - name: Build
      #run: cargo build --release
      run: |
        mkdir -p target/release
        echo "echo helloworldnode" > target/release/encointer-node-notee
        chmod +x target/release/encointer-node-notee
        echo "echo helloworldclient" > target/release/encointer-client-notee
        chmod +x target/release/encointer-client-notee
    - uses: actions/upload-artifact@v2
      with:
        name: client_and_node
        path: | 
          target/release/encointer-node-notee
          target/release/encointer-client-notee
  test-bootstrapping:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - uses: actions/checkout@master
    - name: download build artifacts
      uses: actions/download-artifact@master
    - name: start dev node
      run: /home/runner/work/encointer-node/encointer-node/client_and_node/target/release/encointer-node-notee --tmp --dev &
    - name: get-phase
      run: /home/runner/work/encointer-node/encointer-node/client_and_node/target/release/encointer-client-notee get-phase
    - name: next-phase
      run: /home/runner/work/encointer-node/encointer-node/client_and_node/target/release/encointer-client-notee next-phase
    - name: bootstrap community
      run: |
        cd client
        ./bootstrap_demo_community.sh
  test-community-growing:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - uses: actions/checkout@master
    - name: download build artifacts
      uses: actions/download-artifact@master
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: install py dependencies
      run: |
        python -m pip install --upgrade pip
        pip install geojson random_word pyproj
    - name: start dev node
      run: /home/runner/work/encointer-node/encointer-node/client_and_node/encointer-node-notee --tmp --dev &
    - name: grow community
      run: |
        cd client
        ./bot-community.py init
        ./bot-community.py run

